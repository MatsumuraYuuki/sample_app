<%= form_with(model: current_user.active_relationships.find_by(followed: @user),
# html: { method: :delete } によって、DELETE リクエストを送信することを指定
              html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
